using Lyra;
using UnityEngine;

namespace Triheroes.Code
{/*
    public class circuit : health_system.primary {
        HP4vessel blue;

        public circuit ( int point_count ) {
            blue = new HP4vessel ( point_count );
        }

        public override void damage ( damage damage ) {
            
            int quarter_count = Mathf.FloorToInt ( ( damage.value + eps ) / HP4.one.value );

            blue.damage ( quarter_count );
        }

        protected override void _step() {
            blue.tick ( Time.deltaTime );
        }
    }*/

}